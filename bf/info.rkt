#lang setup/infotab

(define name "bf: a brainf**k compiler, written in Racket")
(define categories '(devtools))
(define can-be-loaded-with 'all)
(define required-core-version "5.1.1")
(define version "1.0")
(define repositories '("4.x"))
(define scribblings '())
(define primary-file "language.rkt")
(define blurb
  '("Prodies support for the brainf**k language."))
(define release-notes
  '((p "First release")))

